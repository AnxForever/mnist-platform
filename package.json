{
  "name": "frontend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\"",
    "dev": "live-server --port=8080 --entry-file=index.html --open",
    "start": "live-server --port=8080 --entry-file=index.html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@agentdeskai/browser-tools-mcp": "^1.2.1",
    "live-server": "^1.2.2"
  }
}
